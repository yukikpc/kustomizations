apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

nameSuffix: -example

commonLabels:
    app: phabricator-example

resources:
  - ingress.yaml

patchesStrategicMerge:
  - statefulset.yaml

patchesStrategicMerge:
  - statefulset.yaml
